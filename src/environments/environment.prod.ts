// environment.prod.ts — producción (Vercel)
// Cambia esta URL por la de tu backend en Vercel después del deploy
export const environment = {
  production: true,
  apiUrl: 'https://dulce-backend.vercel.app/api',
};
